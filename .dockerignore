# Ignore node_modules directory
node_modules

# Ignore frontend directory (handled by its own Dockerfile)
frontend

# Ignore development logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Ignore environment files (should be passed via docker-compose)
.env

# Ignore Docker files themselves if they are in the context
Dockerfile
docker-compose.yml
.dockerignore

# Ignore Git directory
.git
.gitignore

# Ignore uploads directory (mounted as a volume)
uploads/

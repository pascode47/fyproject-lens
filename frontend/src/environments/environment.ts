export const environment = {
    production: false,
    apiUrl: '/api',  // Relative path for API requests via Nginx proxy
    useMockData: false  // Set to false to use real API instead of mock data
  };
